# 找出list中出现次数大于count的元素
def find_count_list(the_arry, count):
    d = {}
    result = []
    for i in range(len(the_arry)):
        d[the_arry[i]] = d.get(the_arry[i], 0) + 1

    for k in d:
        if d[k] > count:
            result.append(k)
    return result
